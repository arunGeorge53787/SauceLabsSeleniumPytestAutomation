

ids=dict(
    uNameField="user-name",
    pWordField="password",
    loginBtn="login-button"
)

xpaths=dict(
    botImg="//img[@class='bot_column']",
    logo="//div[@class='login_logo']",
    errorCloseBtn="//button[@class='error-button']//*[name()='svg']"
)

tagNames=dict(
    warningLabel="h3"
)

warningTexts=dict(
    noUserNameNoPassword="Epic sadface: Username is required",
    noPassword="Epic sadface: Password is required",
    noUsername="Epic sadface: Username is required",
    incorrectUsername="Epic sadface: Username and password do not match any user in this service",
    incorrectPassword="Epic sadface: Username and password do not match any user in this service",
    lockedOutUser="Epic sadface: Sorry, this user has been locked out."
)


creds=dict(
    std_user="standard_user",
    locked_out_user="locked_out_user",
    problem_user="problem_user",
    performance_glitch_user="performance_glitch_user",
    error_user="error_user",
    visual_user="visual_user",
    pWord="secret_sauce"
)